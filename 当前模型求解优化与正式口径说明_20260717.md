# 当前模型求解优化与正式口径说明

## 1. 文档目的

本文档说明 2026-07-17 推送到
`z9876543211234/port_h2_robust_certificate_clean` 的当前模型代码、求解优化范围、
未实施方案、冻结参数口径和已完成的验证。

本轮优化的原则是：

1. 不改变原两阶段鲁棒模型的变量、目标、可行域和不确定集语义；
2. 不把限时解、启发式解或未完成分区覆盖的结果包装为严格证书；
3. 优先解决“每个分区缺少可行见证、无法断点恢复、已知场景与对手问题不一致”的工程问题；
4. 对尚未完成有限界证明的对偶乘积凸包化保持关闭。

## 2. 正式模型语义没有改变

本次修改是求解与证书工程优化，不是物理模型改写。当前口径为：

- AGV 集装箱作业量和 LOHC 外运量仍通过连续等效运力变量表示；
- 保留原运力不等式，即实际任务量不得超过申报的 AGV 运力；
- 未把上述不等式改为等式，也未对 AGV 变量做消元；
- 集装箱和 LOHC 作业 AGV 都按正的运行成本计价，当前为
  `60 CNY/(vehicle·h)`；
- C3 只将 34 辆车的两类工作组容量固定为整数 `29/5`，每时段实际使用的
  AGV 数仍是连续等效运力；
- C1–C3 仍使用两阶段 C&CG，追补问题仍为 LP，正式对手仍基于该 LP 的对偶结构；
- 不确定集、风电选择器、船期延误预算和“最多 2 艘、每艘最多延误 1 小时”的语义不变。

输入文件仍沿用历史字段名 `outbound_mode: agv_virtual_pipeline`。该标签仅是兼容旧配置的
名称，当前数学含义是“LOHC 罐箱外运量受 AGV 等效运力限制”，不应解释为物理管道运输。
本次为避免影响配置兼容性，未对该历史标签做重命名。

## 3. 已实施的求解优化

### 3.1 返回每个分区的完整可行选择器

`_partition_feasibility()` 不再只返回 Gurobi 状态，而是返回
`PartitionFeasibility(status, selector)`。因此，即使当前 C&CG 场景池中没有与某个分区
兼容的历史场景，该分区也会拥有一个合法的完整不确定性选择器。

主要实现：

- `src/port_h2_certificate/partition_oracle.py`

### 3.2 每叶原始—对偶见证回放

对每个可行叶，代码现在会：

1. 固定该叶的完整选择器；
2. 求解固定场景下的原始追补 LP；
3. 求解同一场景下的对偶 LP；
4. 检查原始目标、对偶目标、原始—对偶间隙和最大约束残差；
5. 把该见证的目标值作为该叶已认证的可行下界。

该见证只是“经 primal-dual replay 认证的可行下界”，不代替叶内全局最优证明。
每个非空叶仍必须由 Gurobi 返回 `OPTIMAL`。

### 3.3 合法的叶目标下界和完整 MIP start

对手模型中加入的是：

\[
Q_\ell(z,\pi) \ge \underline Q_\ell^{\mathrm{witness}}-10^{-7},
\]

其中 \(\underline Q_\ell^{\mathrm{witness}}\) 来自同一叶内已通过原始—对偶回放的可行场景。
这不是“全局 \(L+\varepsilon\) 支配剪枝”，不会因为其他叶的当前下界而删除本叶。

同时向对手 MIP 提交完整启动向量：

- 全部二元选择器；
- 全部行对偶变量；
- 全部上界对偶变量；
- 由上述值计算得到的 selector-dual 乘积变量起点。

已知 C&CG 场景与可行性求解器返回的场景都可以作为候选见证，代码选择目标值
最高且已认证的见证作为该叶启动。

### 3.4 771 个船期语义分区

当前冻结船期输入中，有 10 个非零到港源时段，最多延误 2 艘船，每艘可延误
1–4 个 15 分钟时段。其中一个到港时段含 3 艘船，使得同一源时段内还有额外的
两艘延误组合。完整可行投影数为：

\[
1+10\times4+\binom{10}{2}4^2+\binom{5}{2}=771.
\]

`build_ship_delay_partition_assignments()` 直接从当前 `UncertaintyBundle` 的 one-delay 约束、
延误船预算和其他船期约束构造这 771 个可行投影，而不是在代码中写死计数公式。

每个船期投影是一个精确叶，风电二元选择器仍保留在叶内对手 MIP 中。因此，
“771 个叶”不等于枚举了全部风电场景，也不是将风电不确定性改为确定值。

### 3.5 叶级进度、证据指标和断点恢复

每个已完成叶现在记录：

- 叶序号和总叶数；
- `OPTIMAL` / `INFEASIBLE` / 未解析状态；
- 目标值和目标界；
- 见证原始目标、对偶目标、间隙和最大残差；
- Gurobi `Runtime`、`Work` 和节点数。

断点文件只恢复已完整解析的叶，且必须同时匹配：

- C&CG 迭代号；
- 联合不确定集 Bundle SHA-256；
- 分区键列表；
- 当前第一阶段解 SHA-256。

因此，参数、不确定集、迭代或第一阶段解发生变化时，不会错误复用旧叶。
正在求解但尚未达到最终状态的单个叶不保存 Gurobi 分支树，恢复后需从已保存见证重新求解该叶。

### 3.6 叶内模型复用和安全目标上界

在同一次 `solve_exact_partition()` 调用内，对手模型只编译一次。叶之间通过：

1. `model.reset(0)` 清除上一叶求解状态；
2. 增加当前叶固定约束和见证下界；
3. 求解完成后移除临时约束；

进行复用。该复用限于同一 C&CG 迭代内，未跨 C&CG 迭代、未跨 C1–C4 复用。

另外，代码可以从有限原始追补变量界推导一个保守的追补目标上界，并将其作为对手目标的
合法上界。这个上界不是对每个 \(g_j(\pi)\) 的有限 \(L_j/U_j\) 计算，也没有把
indicator 乘积改为 Big-M 或四条凸包约束。

### 3.7 C&CG 证书与 C4 压力分支

C&CG 结果新增记录：

- 是否使用精确分区对手；
- 每次迭代的精确叶数；
- 是否所有叶均为 `OPTIMAL` 或 `INFEASIBLE`；
- 分区覆盖是否完整；
- 已知场景原始回放是否通过。

C4 新增了专门的 `stress_infeasible` 导出分支。当名义第一阶段最优解在压力不确定集下
无法通过 Phase-I 可行性检查时，导出文件会保留 Phase-I 对手值、固定场景回放值、
可行容差和对偶回放容差，而不伪造一个不存在的可行追补调度。

## 4. 明确未实施的方案

为避免把评审建议误写成已实施事实，当前正式路径明确不包括：

| 方案 | 当前状态 | 原因 |
|---|---|---|
| 用有限 \(L_j/U_j\) 四条凸包约束替换 indicator | 未启用 | 尚未完成全部对偶乘积的全局有限界证明 |
| 人工 Big-M | 未启用 | 不允许在辅助 LP 无界时猜测 Big-M |
| 全局 \(L+\varepsilon\) 支配剪枝 | 未启用 | 当前要求严格覆盖，该剪枝会误删可改善全局值但小于 \(L+\varepsilon\) 的叶 |
| 风电三状态重构 | 未启用 | 尚未完成敏感度共享与枚举等价验证 |
| 船舶累计延误整数变量 | 未启用 | 当前 Bundle 编译器仍只接受二元 selector |
| 跨 C&CG 迭代或跨算例的对手模型缓存 | 未启用 | 需要完整 IR、成本、界、Bundle、分区和缩放版本哈希协议 |
| 将 AGV 运力不等式改为等式 | 未实施 | 会改变追补模型结构，不属于本轮数学等价优化 |
| 对 `336/16128` 等系数手工缩放 | 未新增 | 当前代码未硬编码截断小数；本轮不额外改写原 IR 系数 |

`compile_adversary(..., strengthen_with_primal=True)` 为受控实验接口，当前正式分区路径显式记录
`primal_dual_strengthening_used=False`。该实验接口不能被解释为生产证书已使用原始—对偶联合块。

## 5. 当前 20 MW / 34 AGV 冻结输入口径

完整文件 SHA-256 见
[`experiments/base_load_20mw_agv34_c1_c3_20260717/INPUT_HASHES.md`](experiments/base_load_20mw_agv34_c1_c3_20260717/INPUT_HASHES.md)。

### 5.1 主要物理参数

| 参数 | 当前值 |
|---|---:|
| 时域 | 96 期，15 min/期 |
| 港口基础负荷 | 20 MW，96 期恒定 |
| AGV 总数 | 34 |
| 充电机 | 18 |
| 单车电池 / 充电功率 / 作业功率 | 350 kWh / 150 kW / 18 kW |
| AGV SOC 初值 / 下界 / 上界 / 期末下界 | 0.70 / 0.20 / 0.90 / 0.70 |
| 集装箱等效运力 | 4 TEU/(辆·h)，即 1 TEU/15 min |
| LOHC 等效运力 | 1344 kg-H2eq/(辆·h)，即当前标定下 1 TEU/h |
| 充电调整上限 / 充电数爬坡 | 900 kW / 4 辆/期 |
| 购电 / 售电联络线容量 | 100 MW / 300 MW |
| 购电分时电价 | 0.41 / 0.63 / 0.75 CNY/kWh |
| 售电价 | 对应购电价的 50% |
| 电转氢单耗 | 51.6 kWh/kg-H2 |
| 风电直接登陆 : 转氢 | 3 : 7 |
| H2 初值 / 容量 / 期末下界 | 5 / 25 / 5 t-H2 |
| LOHC 初值 / 容量 / 期末下界 | 2 / 12 / 2 TEU，即 2688 / 16128 / 2688 kg-H2eq |
| LOHC 反应釜范围 / 爬坡 / 调整上限 | 0.2–18 MW / 3 MW/期 / 1.2 MW |
| LOHC 转化单耗 / 收率 | 1.8 kWh/kg-H2eq / 0.98 |

### 5.2 经济参数

| 参数 | 当前值 |
|---|---:|
| AGV 作业运行成本 | 60 CNY/(辆·h) |
| 积压时间惩罚 | 12.5 CNY/(TEU·h) |
| 期末积压惩罚 | 300 CNY/TEU |
| 充电调整成本 | 0.3 CNY/kWh |
| 电网偏差成本 | 0.3 CNY/kWh |
| LOHC 调整成本 | 0.3 CNY/kWh |
| 日前 / 日内 / 偏差弃风成本 | 0.1 / 0.1 / 0.1 CNY/kWh |
| LOHC 外运补贴 | 20 CNY/kg-H2eq |
| LOHC 非电操作成本 | 0 |
| LOHC 爬坡成本 | 0.01 CNY/kW |

LOHC 非电操作成本的 0 是已确认的模型口径；AGV 运行成本、调整成本、电网偏差和三项弃风
成本均为正值，未被本次优化静默置零。

## 6. C3 整数分组比例的选择

C3 先在 C1 已发现的完整场景池上扫描集装箱组占比 50%–85%，步长 5%。
分组容量使用 `ROUND_HALF_UP`，LOHC 组等于车队总数减去集装箱组。

扫描选择了 85% 集装箱占比，对应：

- 集装箱组容量：29 辆；
- LOHC 组容量：5 辆。

扫描只是基于 C1 已发现场景池的参数选择诊断，不是鲁棒证书。选中的 29/5 案例后续又单独
通过了完整 771 叶精确对手检查。扫描汇总见：

- `experiments/base_load_20mw_agv34_c1_c3_20260717/c3_ratio_scan/scan_summary.csv`
- `experiments/base_load_20mw_agv34_c1_c3_20260717/c3_ratio_scan/scan_manifest.json`

## 7. 推送前的本地正式运行证据

下表是推送前本地工作树中已完成算例的核心证书摘要。由于这些求解是在待提交改动尚未生成新 commit
时运行，原始结果中记录的 Git HEAD 仍是上一个提交。因此本次不把整个本地结果目录冒充为新提交的
不可变复现产物；新 commit 的正式结果应在该 commit 上重跑后归档。

| 算例 | 完成情况 | 下界 | 上界 | 相对间隙 | 叶状态 | 最大残差 |
|---|---|---:|---:|---:|---|---:|
| C1 | 3 次 C&CG 迭代，`engineering_optimal` | -1445.761839 | -1445.619835 | 9.8221e-5 | 771 叶全部 `OPTIMAL/INFEASIBLE` | 1.8154e-12 |
| C3 | 2 次 C&CG 迭代，`engineering_optimal` | -1410.298613 | -1410.194108 | 7.4101e-5 | 771 叶全部 `OPTIMAL/INFEASIBLE` | 1.1582e-12 |
| C4 | 名义解后压力检查进入 `stress_infeasible` 分支 | — | — | — | Phase-I=1.3374e-8，回放=0 | — |

C4 的 Phase-I 值仅比 `1e-8` 可行容差高 `3.3740e-9`，而固定场景原始回放为 0。
这是边界数值情形，不应被表述为强物理不可行证据；导出分支的作用是完整披露该数值事实。

C2 的正式分区运行在本次打包时尚未归档为完整证书。正在增长的 `formal_C2/` 目录未纳入本次提交，
避免在运行中截取一个并非完整证书的状态。

## 8. 测试与验证

提交前执行：

```bash
PYTHONPYCACHEPREFIX=/tmp/port_h2_pycache_push_20260717 \
PYTHONPATH=src:. \
/opt/anaconda3/bin/python -m pytest -q
```

结果：

```text
142 passed in 57.97s
```

C3 扫描脚本位于实验目录，不属于默认 `testpaths`，因此另行显式执行其测试：

```text
12 passed in 0.93s
```

新增或扩展的验证覆盖：

- 可行选择器返回与每叶见证；
- 完整 MIP start 的变量覆盖；
- 已知场景下界与回放一致性；
- 771 个船期语义分区的构造与覆盖；
- 断点哈希匹配和已完成叶恢复；
- C&CG 分区结果和证书字段；
- C4 `stress_infeasible` 导出；
- 命令行入口的语义分区与恢复选项。

## 9. 复现入口

从仓库根目录运行 C1：

```bash
PYTHONPATH=src:. /opt/anaconda3/bin/python -m runners.run_case \
  experiments/base_load_20mw_agv34_c1_c3_20260717/inputs/base20mw_agv34/C1_base20mw_agv34.yaml \
  --profile quarter_hour_96 \
  --output experiments/base_load_20mw_agv34_c1_c3_20260717/formal_C1_reproduced \
  --semantic-ship-partition \
  --resume-partition
```

C2 使用 `C2_base20mw_agv34.yaml`，C3 使用 `C3_base20mw_agv34_selected.yaml`，并保留同样的语义分区和恢复参数。

C4 不使用语义分区选项：

```bash
PYTHONPATH=src:. /opt/anaconda3/bin/python -m runners.run_case \
  experiments/base_load_20mw_agv34_c1_c3_20260717/inputs/base20mw_agv34/C4_base20mw_agv34.yaml \
  --profile quarter_hour_96 \
  --output experiments/base_load_20mw_agv34_c1_c3_20260717/formal_C4_reproduced
```

### 严格证书接受条件

正式 C1–C3 仅在以下条件全部满足时接受：

1. 主问题、Phase-I 对手和成本对手都返回要求的最终状态；
2. 771 个船期分区全部完成；
3. 所有非空叶为 `OPTIMAL`，空叶为 `INFEASIBLE`；
4. 不使用 time limit 结果生成证书；
5. 原始回放、对偶回放、界顺序、残差和不确定集覆盖检查全部通过。

## 10. 本次 Git 提交的边界

纳入提交：

- 当前正式求解、分区、检查点、证书导出和 CLI 代码；
- 对应单元/集成测试；
- 20 MW / 34 AGV 的 C1–C4 冻结输入包和 SHA-256 清单；
- C3 50%–85% 整数分组扫描脚本、测试、汇总和选择清单；
- 本中文说明。

明确不纳入提交：

- 正在增长的 C2 检查点和未完整结果；
- C1/C3/C4 的整个本地求解目录；
- `__pycache__`、临时诊断、旧扫描和保存的压缩包；
- 未接入当前正式路径的 `adversary_reformulation.py` 实验草案及其测试；
- 工作树中原已存在、与本次优化无关的两个船期 CSV 改动。

这一边界保证 GitHub 上的内容对应“当前实际运行的正式模型路径”，不混入未完成产物或未启用实验。
